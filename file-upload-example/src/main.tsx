import React from 'react'
import ReactDOM from 'react-dom/client'
import FileUploadPilot from './components/FileUploadPilot.tsx'
import './index.css'

ReactDOM.createRoot(document.getElementById('root')!).render(
  <React.StrictMode>
    <FileUploadPilot />
  </React.StrictMode>,
)
